{
  "name": "meshtool",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpx parcel ./index.html --open --no-cache",
    "build": "pnpx parcel build ./index.html --public-url ./ --no-cache",
    "start": "pnpx parcel serve ./index.html --open --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "three": "^0.175.0"
  },
  "devDependencies": {
    "@types/three": "^0.175.0",
    "buffer": "^6.0.3"
  }
}
